## Process this file with automake to produce Makefile.in

plugindir = $(pkglibdir)/plugins/highlight-selected

plugin_PYTHON = \
	highlight-selected.py

plugin_DATA = \
	highlight-selected.plugin

EXTRA_DIST = \
	$(plugin_DATA)
